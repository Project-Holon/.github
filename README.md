# Project Holon: organisation profile (public)

This repository exists because GitHub renders an organisation's front page
from a repository named `.github` (public view) or `.github-private`
(members-only view), file `profile/README.md`. This one is the public view.
There is nothing else here.
